# 班级考勤系统 — 需求分析

> 基于 `实验任务` 原始要求 + `request.md` 评分标准的综合需求分析

---

## 一、项目背景与目标

### 1.1 原始实验任务

> 以**内容安全领域的人脸识别、活体检测技术**为核心，结合 BS（Browser/Server）架构开发，实现一套功能完整、安全可靠的班级考勤系统。要求融入合照学生识别、学生面部情绪分析功能。

关键词解读：

| 关键词 | 含义 | 对应实现 |
|---|---|---|
| 内容安全 | 防止虚假考勤（照片/视频伪造），确保签到数据真实 | 活体检测是核心防线 |
| 人脸识别 | 检测+比对，确认签到者身份 | 检测(SCRFD) + 识别(ArcFace) |
| 活体检测 | 区分真人与照片/屏幕/视频 | 11 维综合评分方案 |
| BS 架构 | 浏览器端操作，服务端处理 | Vue 3 + FastAPI |
| 合照识别 | 批量检测+识别，生成参与名单 | 合照上传→SCRFD 全检测→逐一比对 |
| 情绪分析 | 同步分析面部表情，分类统计 | HSEmotion + DeepFace 混合 |

### 1.2 考察维度

```
┌──────────────────────────────────────────────────────┐
│                   考核重点分布                         │
├────────────┬─────────┬──────────┬────────┬──────────┤
│  活体安全   │ 人脸识别 │ 合照批量  │ 情绪分析 │ 工程能力  │
│  (15分)    │ (10分)  │  (9分)   │ (8分)  │ (58分)   │
│            │         │          │        │          │
│ rPPG脉搏波 │ SCRFD   │ 批量检测  │ 三引擎  │ BS架构   │
│ 摩尔纹检测 │ ArcFace │ 去重匹配  │ 混合    │ 数据库   │
│ 光流分析   │ 512d    │ 活动统计  │ 7类分类 │ API设计  │
│ 动态阈值   │ 余弦    │ 报表导出  │ 统计图  │ 前后端   │
│ DeepFace  │ 相似度  │          │        │ 权限     │
│ 动作挑战   │        │          │        │ 异常处理 │
└────────────┴─────────┴──────────┴────────┴──────────┘
```

---

## 二、功能需求

### F1 基础考勤功能（25分）

**原始要求**：
> 前端通过浏览器调用摄像头采集学生人脸信息，传输至后端；后端完成活体检测、人脸库比对，将考勤结果返回至前端渲染显示。

#### 定性分析

基础考勤是系统的**核心业务闭环**，决定了系统"能"还是"不能"。

**业务场景**：某日某课程的上课时间，教师开启考勤，学生依次（或自动连续）通过浏览器摄像头完成签到。每一次签到是一个独立的事务——采集人脸、验证活体、比对身份、记录结果、返回展示。

**四层结构**：

```
┌─────────────────────────────────────────────────────────┐
│ 第一层：采集层（前端 → 后端传输）                          │
│                                                         │
│   摄像头 (getUserMedia) → 拍照 (canvas) → HTTP 上传       │
│   关键约束：720p 以上、JPEG 0.95 质量、< 2 秒传输          │
│   异常情况：摄像头被占用、权限被拒、画面过暗/过亮            │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ 第二层：分析层（后端 AI 管线）                             │
│                                                         │
│   人脸检测 (SCRFD) ─┬→ 活体检测 (11维) → 真人/欺骗        │
│                    ├→ 特征提取 (ArcFace 512d)             │
│                    └→ 情绪分析 (三引擎) → 7类情绪          │
│                                                         │
│   关键约束：检测→识别→活体→情绪 需在 < 3 秒内完成          │
│   异常情况：未检测到人脸、多张人脸、质量不足、识别超时       │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ 第三层：比对层（身份确认）                                  │
│                                                         │
│   512d 特征 ──余弦相似度──→ 与全库候选人逐一比对 → 最高分   │
│                                                         │
│   关键约束：阈值 0.58（人脸识别）+ 0.55 动态（活体检测）     │
│   异常情况：无人脸库（候选集为空）、最高分低于阈值、多人接近   │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ 第四层：记录层（持久化 + 展示）                             │
│                                                         │
│   写入 AttendanceRecord ─┬→ 前端实时渲染结果               │
│                         ├→ EmotionRecord (成功时)         │
│                         └→ 供统计分析模块查询聚合           │
│                                                         │
│   关键约束：记录必须含：成功/失败、学生信息、置信度、活体分、  │
│            情绪类型、课程名、时间、失败原因                  │
└─────────────────────────────────────────────────────────┘
```

**依赖关系**：

```
                   ┌─────────────┐
                   │  人脸库      │ ← 学生管理模块维护
                   │  (students) │    录入/批量导入/增删改
                   └──────┬──────┘
                          │ 提供候选特征
                          ▼
    摄像头 ──→ 分析层 ──→ 比对层 ──→ 记录层
                          │            │
                          │ 活体验证    │ 写入记录
                          ▼            ▼
                    ┌──────────┐  ┌──────────────┐
                    │ 动作挑战  │  │ 统计分析模块  │ ← 聚合查询/报表
                    │ (随机4种) │  │ (statistics) │
                    └──────────┘  └──────────────┘
```

**设计要回答的关键问题**：

| 问题 | 答案 |
|---|---|
| 考勤的"上下文"是什么？ | 课程名称（course_name），由教师在前端输入，存入记录 |
| 一次考勤绑定一个学生还是多个？ | **一个**——摄像头画面中只允许一张人脸 |
| 活体检测在哪个环节？ | 比对之前——先确认是活人，再比对身份 |
| 识别失败是否也记录？ | **是**——失败记录同样入库，含失败原因 |
| 和统计分析的关系？ | 考勤记录是统计分析的数据源，按课程+日期+学生三维聚合 |

#### 当前实现逻辑

**数据流**：

```
浏览器摄像头 ──→ canvas 拍照 ──→ HTTP 上传 ──→ 后端
                                                    │
  前端渲染 ←── JSON 响应 ←── 判定 ←── 活体检测(11维)
    │                                        │
    ├ 成功：学号/姓名/班级/置信度/情绪/活体分    └── 人脸比对(512d 余弦)
    └ 失败：具体原因(活体不过/未匹配/动作未完成)
```

**需求分解**：

| 编号 | 需求 | 分值 | 说明 |
|---|---|---|---|
| F1.1 | 摄像头调用正常、人脸采集流畅 | 3 | `getUserMedia` API，720p，支持 Chrome/Firefox/Edge |
| F1.2 | 支持手动/自动捕捉并显示采集状态 | 3 | 手动拍照 + 3 秒倒计时 + 连续自动捕捉三模式 |
| F1.3 | 实时渲染后端返回的学生信息 | 3 | 学号/姓名/性别/班级/置信度/情绪/活体分/时间 |
| F1.4 | 支持筛选查询考勤记录 | 3 | 按姓名/学号关键词 + 成功/失败状态筛选，分页排序 |
| F1.5 | **活体检测可抵御照片、视频欺骗** | **10** | 核心得分项：多维度防欺骗，11 维综合评分 |
| F1.6 | 班级人脸库批量导入、增删改 | 2 | 文件名解析(学号-姓名-班级-性别)，单张/批量上传 |
| F1.7 | 记录考勤数据并 Excel 导出 | 1 | 全量导出 + 课程维度统计导出 |

**F1.5 活体检测详细要求**（10 分核心项）：

| 检测维度 | 权重 | 对抗目标 |
|---|---|---|
| DeepFace MiniFASNet 深度学习 | 40% | 通用照片/视频翻拍 |
| rPPG 脉搏波（绿色通道 FFT） | 15% | **照片/视频无心跳信号** |
| 光流时序一致性 | 12% | **手持照片晃动（刚体平移）** |
| 摩尔纹专项 2D-FFT | 10% | **屏幕翻拍干涉条纹** |
| FFT 频域分析 | 5% | 通用频谱衰减异常 |
| LBP 纹理分析 | 5% | 纸张/屏幕纹理异常 |
| 色彩/清晰度/亮度/边缘 | 各 2-3% | 辅助判断 |
| 动态阈值 | — | 自适应光照变化 |

**动作挑战机制**：4 种随机动作（微笑/左转/右转/张嘴），15 秒过期，后端强制验证。rPPG < 0.15 一票否决。

---

### F2 合照学生识别功能（9分）

**原始要求**：
> 支持上传班级/活动大合照，后端对照片中的人脸进行批量识别，匹配人脸库中学生信息，生成匹配成功的学生名单，统计学生参与各类活动的频次，形成统计报表。

**数据流**：

```
上传合照 ──→ SCRFD 全检测（所有人脸框）
                │
                ├→ 逐人脸 ArcFace 提取 512d 特征
                ├→ 与数据库全部候选人余弦比对
                ├→ 去重（同一学生只计一次）
                ├→ 同步情绪分析
                ├→ 写入 AttendanceRecord + ActivityParticipation
                │
                └→ 返回 { 检测人脸数, 成功识别数, 结果列表 }
```

**需求分解**：

| 编号 | 需求 | 分值 | 说明 |
|---|---|---|---|
| F2.1 | 前端上传合照，后端批量识别人脸 | 3 | 拖拽/选择上传，预览后识别 |
| F2.2 | 准确率 ≥85%，处理 10-50 人，生成名单 | 3 | ArcFace 512d + 余弦相似度 + 阈值 0.58 |
| F2.3 | 自动记录每位学生活动参与次数 | 2 | ActivityParticipation 表，JOIN + GROUP BY |
| F2.4 | 生成统计报表（数字、表格、柱状图） | 1 | Dashboard 卡片 + 表格 + ECharts 柱状图 |

---

### F3 情绪分析功能（8分）

**原始要求**：
> 在考勤、合照识别过程中，同步对学生面部表情进行分析，并对情绪进行分类，可在前端查看统计结果。

**同步处理点**：

| 场景 | 触发位置 | 备注 |
|---|---|---|
| 考勤签到 | `attendance.py:88` — `emotion_service.analyze()` | 签到成功后记录 |
| 合照识别 | `group_photo.py:50` — `emotion_service.analyze()` | 每张识别成功的人脸 |
| 情绪记录 | `EmotionRecord` 表 — 含 student_id / timestamp / type / confidence / source | source 区分来源 |

**需求分解**：

| 编号 | 需求 | 分值 | 说明 |
|---|---|---|---|
| F3.1 | 同步提取面部特征 | 2 | 在考勤和合照流程中无需额外操作 |
| F3.2 | 完成情绪分类（7 类） | 3 | HSEmotion（主力）+ DeepFace（复核）+ OpenCV（保底） |
| F3.3 | 后端记录完整情绪数据 | 2 | EmotionRecord 表：学号/姓名/时间/类型/置信度/来源 |
| F3.4 | 前端查看统计结果 | 1 | ECharts 饼图 + 明细表格，点击可查学生详情 |

**7 类情绪**：happy / sad / angry / surprised / fearful / disgusted / neutral

**三引擎决策链**：
```
HSEmotion (EfficientNet-B0) → 置信度 ≥0.6 → 直接返回
        │ 不足
        ▼
DeepFace (EmotionNet) → 成功 → 返回
        │ 失败
        ▼
OpenCV 启发式 → smile cascade / eye cascade / Sobel 梯度
```

---

### F4 系统安全性能（18分）

**原始要求**：
> 优化识别算法，提升活体检测、人脸比对、合照识别的准确率，降低误识别率；实现系统异常处理。

**需求分解**：

| 编号 | 需求 | 分值 | 说明 |
|---|---|---|---|
| F4.1 | 活体检测抗照片伪造 | 7 | rPPG 一票否决 + DeepFace + LBP 纹理 + 摩尔纹 |
| F4.2 | 活体检测抗视频伪造 | 8 | 随机动作挑战 + rPPG + 摩尔纹 + 光流时序 |
| F4.3 | 实现教师/学生账号权限区分 | 3 | JWT + bcrypt + require_teacher 依赖注入 |

**攻击防御矩阵**：

| 攻击方式 | 拦截层 | 失效条件 |
|---|---|---|
| 打印照片 | rPPG（无脉搏）+ LBP（纸纹理）+ DeepFace | 高质量照片 + 真人站在旁边（rPPG 仍可穿透） |
| 手机/平板屏幕翻拍 | rPPG + 摩尔纹（干涉条纹）+ DeepFace | 极端高分辨率屏幕 |
| 视频重放（录别人） | rPPG + 摩尔纹 + **随机动作挑战** | 攻击者恰好录制了正确动作+高保真 |
| 手持照片晃动 | 光流时序（刚体平移检测） | 模拟真人的非刚体晃动 |
| 快速闪现照片 | 多帧分析（rPPG 需 30 帧以上） | 单帧提交绕过 |
| 过曝/过暗 | 动态阈值自动适应 | DeepFace 模型本身失效 |

**权限矩阵**：

| 操作 | 教师 | 学生 |
|---|---|---|
| 签到 | ✅ | ✅ |
| 查看考勤记录 | ✅（全部） | ✅（仅自己） |
| 增删改学生 / 导入人脸 | ✅ | ❌ |
| 合照识别 | ✅ | ❌ |
| 查看统计 | ✅ | ✅ |

---

## 三、非功能需求

| 编号 | 需求 | 指标 | 当前方案 |
|---|---|---|---|
| NF1 | 人脸识别置信度 | ≥ 58%（可配置） | ArcFace 512d 余弦相似度，`face_threshold=0.58` |
| NF2 | 合照识别准确率 | ≥ 85%，支持 10-50 人 | SCRFD + ArcFace，去重逻辑 |
| NF3 | 活体检测抗攻击 | 抵御打印照片/屏幕翻拍/视频重放 | 11 维评分 + rPPG 一票否决 |
| NF4 | 浏览器兼容 | Chrome / Firefox / Edge | `getUserMedia` API + Element Plus |
| NF5 | 人脸检测响应 | < 2 秒 | SCRFD-10GF CPU 推理 ~200ms |
| NF6 | 部署要求 | 单机可运行，无需 GPU | InsightFace CPU 模式, PyTorch CPU |

---

## 四、架构约束

| 约束 | 说明 |
|---|---|
| BS 分层清晰 | 前端 Vue 3 (views/api/stores/router) + 后端 FastAPI (api/services/models/core) |
| 数据库合理 | 5 表，FK 关联，索引覆盖查询字段 |
| API 规范 | RESTful，JWT 认证，Pydantic 校验，自动生成 OpenAPI 文档 |
| 前后端分离 | Vite 开发代理 → FastAPI 8000 端口 |

---

## 五、评分细则与当前状态逐条对照

### 一、架构要求（10分）

| # | 评分项 | 分值 | 状态 | 当前实现 | 待确认 |
|---|---|---|---|---|---|
| ① | BS分层架构清晰，各层职责明确 | 3 | ⚠️ | 前端 views/api/stores/router 四层；后端 api/services/models/core 四层 | 分层是否被认可？Service 层方法直接写在路由中的情况是否扣分？ |
| ② | 前端适配主流浏览器，界面简洁、交互友好 | 3 | ⚠️ | Element Plus UI，8 个页面，三种拍照模式 | 移动端未适配、无注册页面、前端人脸检测是假实现——这些算不算扣分点？ |
| ③ | 后端API接口规范，可实现核心逻辑交互 | 2 | ⚠️ | RESTful + JWT + Pydantic + OpenAPI 文档 | 响应格式不统一（有的返 list、有的返单对象），分页缺元数据——要改吗？ |
| ④ | 数据库设计合理，数据表关联正确 | 2 | ⚠️ | 5 表 FK 关联，索引覆盖 | face_encoding 512d 存 LargeBinary，无软删除——算合理吗？ |

### 二、核心功能1：基础考勤（25分）

| # | 评分项 | 分值 | 状态 | 当前实现 | 待确认 |
|---|---|---|---|---|---|
| ① | 摄像头调用正常、人脸采集流畅 | 3 | ⚠️ | getUserMedia 1280×720，canvas.toBlob | 无设备选择（多摄像头切换）、无权限引导——扣分吗？ |
| ② | 支持手动/自动捕捉并显示采集状态 | 3 | ⚠️ | 手动+3秒倒计时+连续自动，步骤条+状态栏 | 前端人脸检测用亮度方差模拟——算"采集状态"吗？ |
| ③ | 能实时渲染后端返回的学生信息 | 3 | ⚠️ | el-descriptions 3列：学号/姓名/性别/班级/置信度/情绪/活体分 | 失败时也展示大量 "-" 字段——体验是否合格？ |
| ④ | 支持筛选查询考勤记录 | 3 | ⚠️ | 关键词+状态筛选，前端分页排序，导出 Excel | **缺失**：日期范围筛选、后端分页、单条/批量删除——这些是必要吗？ |
| ⑤ | **活体检测功能正常，可抵御照片、视频欺骗** | **10** | ⚠️ | 11维方案（rPPG+摩尔纹+光流+DeepFace+动作挑战），代码已写完 | **核心风险**：从未实际测试过！11维真实效果未知。rPPG 对深色皮肤、弱光环境的效果未验证 |
| ⑥ | 班级人脸库可批量导入、单个添加/删除/修改 | 2 | ✅ | CRUD + 文件名解析批量导入 + 重建特征 | 基本到位 |
| ⑦ | 可记录考勤数据并支持Excel格式导出 | 1 | ✅ | 全量+课程统计 xlsx 导出 | 基本到位 |

### 三、核心功能2：合照识别（9分）

| # | 评分项 | 分值 | 状态 | 当前实现 | 待确认 |
|---|---|---|---|---|---|
| ① | 支持前端上传合照，后端可检测并批量识别人脸 | 3 | ⚠️ | 拖拽/选择上传→SCRFD全检测→逐人识别→去重 | 逐人串行处理（50人耗时严重），无"未识别人脸数"统计——扣分吗？ |
| ② | 准确率≥85%，处理10-50人，生成含学号、姓名名单 | 3 | ⚠️ | ArcFace 512d 余弦匹配 | **无 benchmark 数据**，拿什么证明 85%？需要测试集和实验数据 |
| ③ | 可自动记录每位学生活动参与次数 | 2 | ✅ | ActivityParticipation 表 + JOIN 统计 | 已修复写入 |
| ④ | 能生成统计数字、表格、柱状图等形式的统计报表 | 1 | ⚠️ | Dashboard 卡片+表格+饼图，Excel 导出 | **缺柱状图**——明确扣分点 |

### 四、核心功能3：情绪分析（8分）

| # | 评分项 | 分值 | 状态 | 当前实现 | 待确认 |
|---|---|---|---|---|---|
| ① | 考勤、合照识别过程中可同步提取面部特征 | 2 | ✅ | attendance.py:88 和 group_photo.py:50 同步调用 | 基本到位 |
| ② | 可完成情绪分类 | 3 | ⚠️ | HSEmotion+DeepFace+OpenCV 三引擎，7类 | 三引擎未实测；启发式回退准确率低——需要验证数据 |
| ③ | 后端可记录学号、姓名、识别时间、情绪类型的完整数据 | 2 | ✅ | EmotionRecord 表 + source 区分来源 | 基本到位 |
| ④ | 前端支持查看统计结果 | 1 | ✅ | ECharts 饼图+明细表 | 但无日期/课程筛选——扣分吗？ |

### 五、核心功能4：系统安全（18分）

| # | 评分项 | 分值 | 状态 | 当前实现 | 待确认 |
|---|---|---|---|---|---|
| ① | 活体检测抗照片伪造攻击效果良好 | 7 | ⚠️ | 代码层面四重防线(rPPG+DeepFace+LBP+摩尔纹) | **从未实测**——需要打印照片+屏幕翻拍实际测试，给出拦截率 |
| ② | 活体检测抗视频伪造攻击效果良好，杜绝虚假考勤 | 8 | ⚠️ | 代码层面五重防线(+随机动作挑战+光流时序) | **从未实测**——需要录制视频攻击测试，验证随机动作是核心防线 |
| ③ | 实现教师、学生账号权限区分 | 3 | ✅ | JWT + bcrypt + require_teacher + 学生数据隔离 | 基本到位，但无登录失败限制、无操作审计 |

### 六、实验报告（30分）

| # | 评分项 | 分值 | 状态 | 当前实现 | 待确认 |
|---|---|---|---|---|---|
| ① | 报告：结构完整、格式规范，涵盖需求分析、系统设计、代码实现 | 20 | ⚠️ | `docs/课程设计报告.md` 有初步框架 | 缺结果分析、测试数据、截图、部署验证——需要大量补充 |
| ② | 源码完整、注释规范可部署，部署说明清晰 | 10 | ⚠️ | 代码完整，有 README 启动指引 | **依赖未在干净环境完整验证**（pip install 曾失败） |

---

## 六、当前最大风险排序

| 优先级 | 问题 | 影响分值 | 说明 |
|---|---|---|---|
| **P0** | 活体检测从未实测 | **15分** | 抗照片7分+抗视频8分，全凭理论设计，无任何实验数据支撑 |
| **P0** | 合照准确率无 benchmark | **3分** | 拿什么证明 ≥85%？ |
| **P0** | 报告缺结果分析 | **20分** | 无测试数据、截图、对比实验 |
| **P1** | 柱状图缺失 | **1分** | 明确要求"柱状图"，当前只有饼图 |
| **P1** | 考勤记录无删除功能 | 隐含 | 用户体验缺陷 |
| **P1** | 异常处理不完整 | 隐含 | 无统一异常中间件、无超时处理 |
| **P1** | 部署未在干净环境验证 | **10分** | "可部署"的前提是能跑通 |
